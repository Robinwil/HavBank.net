import { addCollection } from '@iconify/svelte';
import heroicons from '@iconify-json/heroicons/icons.json';

// Register the Heroicons collection so @iconify/svelte can render offline.
addCollection(heroicons);
